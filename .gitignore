# 秘密情報（APIキー等）は絶対にコミットしない
.env
# 画面から設定する Stripe 秘密鍵の保存ファイル（コミット厳禁）
stripe_secret.key
/app/stripe_secret.key

# 依存ライブラリ
/vendor/

# ローカルの支払いログ（個人情報を含むためコミットしない）
/logs/*.log

# SQLite データベース（アカウント等を含むためコミットしない）
/data/
